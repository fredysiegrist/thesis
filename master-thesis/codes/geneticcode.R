require(seqinr)
tablecode(latexfile="codontable.tex")
# to plot a codon table with ratios and frequencies with the overwriting function from statanacoseq
require(statanacoseq)
tablecode(mylist(whatout=1))
